# --- Agent (既存 agent.py が必要とするもの) ---
google-adk>=2.1.0
google-genai>=1.75.0
mcp>=1.0.0

# --- Web backend ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0

# pymongo は書き込みバッチ（別系統）用。Webバックエンドの読み出しは MCP 経由なので必須ではないが、
# 日次更新バッチを同一リポジトリに置く想定で入れておく。
pymongo[srv]>=4.6.0


# API利用制限
slowapi>=0.1.9