# --- Agent for Gemini agent.py ---
google-adk>=2.1.0
google-genai>=1.75.0
mcp>=1.0.0

# --- Web backend ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0

# pymongo is for write batches (a separate flow). Web backend reads go through MCP,
# so it is not strictly required, but it is included because the daily update batch
# is expected to live in the same repository.
pymongo[srv]>=4.6.0


# API rate limiting
slowapi>=0.1.9
